# Docker Environment Configuration
# Environment variables for Docker container deployment

# Server Configuration
PORT=8080
ENVIRONMENT=production
LOG_LEVEL=info

# Database Configuration
# Uses docker-compose service name 'postgres' as hostname
DATABASE_URL=postgres://user:password@postgres:5432/todoapi?sslmode=disable

# JWT Configuration
# Generate a strong secret for production use
JWT_SECRET=your-super-secret-jwt-key-that-is-at-least-32-characters-long
JWT_EXPIRATION=24

# CORS Configuration
ALLOWED_ORIGINS=*