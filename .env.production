# Production Environment Configuration
# Copy this file to .env and update the values for production deployment

# Server Configuration
PORT=8080
ENVIRONMENT=production
LOG_LEVEL=warn

# Database Configuration
# Update with your production database credentials
DATABASE_URL=postgres://username:password@your-db-host:5432/todoapi?sslmode=require

# JWT Configuration
# IMPORTANT: Generate a strong, unique secret for production
JWT_SECRET=CHANGE-THIS-TO-A-STRONG-SECRET-AT-LEAST-32-CHARACTERS-LONG
JWT_EXPIRATION=24

# CORS Configuration
# Specify allowed origins for production (comma-separated)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com